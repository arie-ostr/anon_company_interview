def validate_dict(tested_dict:dict,mask:dict) -> dict:
    """
    asserts keys are the same as in mask including values
    asserts dict content evaluates to true with respect to mask
    """
    assert False, "Not implemented yet"